import { expect, test } from 'vitest'
import { blockHTMLtoMarkdown, blockMarkdownToHTML } from "../src/functions";
import { htmlTranslations } from '../src/variables'

// test('blockGetCaretPos', () => {
//   const div = document.createElement('div');
//   div.innerHTML = 'Hello, world!';
//   document.body.appendChild(div);
//   const pos = blockGetCaretPos(div);
//   expect(pos).toBe(0);
//   document.body.removeChild(div);
// });


